{
  "name": "primary-capital-website",
  "private": true,
  "version": "1.0.0",
  "description": "External marketing site for Primary Capital, with Client Login wired to the ChargeOn Payment Portal Experience Cloud site via Salesforce Headless Identity API.",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js"
  }
}
